from doctest import DocTestFinder
from email.headerregistry import AddressHeader
from re import S
from signal import SIG_DFL
from this import d
from tkinter.dnd import dnd_start
from attr import asdict


asdfsafsdf
dfsdfs
asdictf
SIG_DFLsdf
model = keras.models.Sequential([
    keras.layers.Conv2D(64, 7, activation="relu", padding="same", input_shape=p4),
    keras.layers.MaxPooling2D(2),
    keras.layers.Conv2D(p1, 3, activation="relu", padding="same"),
    keras.layers.Conv2D(128, 3, activation="relu", padding="same"),
    keras.layers.MaxPooling2D(2),
    keras.layers.Conv2D(256, 3, activation="relu", padding=p3),
    keras.layers.Conv2D(256, 3, activation="relu", padding="same"),
    keras.layers.MaxPooling2D(2),
    keras.layers.Flatten(),
    keras.layers.Dense(p2, activation="relu"),
    keras.layers.Dropout(0.5),
    keras.layers.Dense(64, activation="relu"),
    keras.layers.Dropout(0.5),
    keras.layers.Dense(10, activation="softmax")
])
AddressHeaderfs

DocTestFinderas
dnd_start

